[defaults]

inventory = inventory
host_key_checking = false
remote_user = ec2_user
private_key_file=/home/runner/.ssh/my_key_pair

[privilege_escalation]

become = true
become_method = sudo
become_user = root
become_ask_pass = false
